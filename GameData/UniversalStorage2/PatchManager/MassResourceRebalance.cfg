PatchManager
{
	// Required settings.  
	// srcPath should use forward slashes, and include the full file name.  srcPath should be in a directory 
	// called ".../PatchManager/PluginData"
	modName = Universal Storage II
	patchName = MassResourceRebalance
	srcPath = UniversalStorage2/PatchManager/PluginData/MassResourceRebalance.cfg
	
	shortDescr = Rebalance mass & resources

	// Optional, but recommended
	longDescr = Rebalance mass and resource amounts to to make craft building easier


	//// Optional entries here
	//exclusions = ShortenName

	// dependencies, only show this patch if these specified mods are available
	// List either the directory of the mod (as show by ModuleManager), or the 
	// mod DLL (as show by ModuleManager)
	//dependencies = 

	// Path to icon, if desired.  Can be a flag
	icon = UniversalStorage2/Flags/UniversalStorage

	// Author's name, if desired
	author = Linuxgurugamer, Sandriell
}

